# Compatible ranges rather than exact pins: several previously-pinned versions
# (pydantic 2.6.1, scikit-learn 1.4.1.post1) no longer publish prebuilt wheels
# for Python 3.9 / arm64, so a fresh install tried to compile from source and
# failed without a Rust toolchain. These ranges resolve to binary wheels.
#
# Verified end-to-end on Python 3.9.6 (arm64) resolving to:
#   fastapi 0.139.2 · uvicorn 0.51.0 · httpx 0.28.1 · pydantic 2.13.4
#   numpy 2.5.1 · scikit-learn 1.9.0 · cachetools 6.2.6 · python-dotenv 1.2.2

fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
httpx>=0.27,<1
scikit-learn>=1.5,<2
numpy>=1.26,<3
python-dotenv>=1.0,<2
pydantic>=2.9,<3
cachetools>=5.3,<7

# NOTE: the optional Claude Science enrichment tier (backend/services/claude_extract.py)
# is intentionally NOT installed by default — see requirements-optional.txt. The running
# dashboard never imports it, so it adds nothing but install time/risk in this phase.
